const CollegeLogo = ({ name }) => {
  return (
    <div className=" flex items-center justify-center ml-3 h-full w-max text-center text-2xl font-semibold text-white uppercase">
      {name}
    </div>
  );
};

export default CollegeLogo;
